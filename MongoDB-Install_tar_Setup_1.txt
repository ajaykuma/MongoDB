#update packages
apt-get update

#machine setup basics
sudo su --- to login as root
apt-get install vim
apt-get install wget
apt-get install ntp
apt-get install openssh-server
apt-get update

ubuntu ---disable firewall
sudo iptables --flush
sudo iptables stop
or
sudo ufw disable

ifconfig ---get your ipaddress
hostname ---get your hostname

update your /etc/hosts
127.0.0.1     localhost
ipaddress     hostname

sudo visudo and add <username> as user next to root where you see ALL
This will give your user sudo access

#check if java installed 
#java -version
#if not
Download oracle jdk from oracle site or downloaded from dropbox link provided
create a directory under /usr/lib
cd /usr/lib
mkdir jvm
cd jvm
sudo tar -xvf /home/mac/Downloads/jdk****

update your java path in your user's .bashrc
su - <username>

(ex: my user mac)
su - mng

vi .bashrc
export JAVA_HOME=/usr/lib/jvm/jdk****
export PATH=$PATH:$JAVA_HOME/bin

save your .bashrc
refresh it by ---> source .bashrc

$java -version ( does it show your java version)

Now Generate ssh keys for your user(mng) on ubuntu

Commands:
--------------
To have SSH access across machines
1.generate ssh keys using 
ssh-keygen -t rsa -P ""

2.Distributing ssh public keys to other machines
----------------------
mng$machine1:ssh-copy-id -i $HOME/.ssh/id_rsa.pub mng@machine2

3.To enable SSH access to your local machine with this newly created key
----------------------------------------
to add the xxxx@master’s public SSH key (which should be in $HOME/.ssh/id_rsa.pub) 
to the authorized_keys file of xxxx@slave(in this user’s $HOME/.ssh/authorized_keys)
cat $HOME/.ssh/id_rsa.pub >> $HOME/.ssh/authorized_keys

#Mongo Setup
#Download tar from https://www.mongodb.com/download-center
--downloaded 3.4.20
--downloaded 4.0.9

#untar them in a location
sudo su

cd /usr/local
tar -xvf /home/<username>/Downloads/mongo*...tar.gz

--create a symlink pointing to a version of mongodb
--while being logged in as root

ln -s mongodb-linux-x86_64-ubuntu1604-3.4.20 mongodb

--change ownership
chown -R mng:mng mongo*

cd 

--switch to your user(say:mng)

--update path in .bashrc for your user
export MONGODB_HOME=/usr/local/mongo/
export PATH=$PATH:$MONGODB_HOME/bin

--refresh .bashrc
source .bashrc
--create directory using sudo privileges --> /data/db and change ownership to your user
--being logged in as your user,run mongod from terminal

$mongod
--start another terminal
$mongo
Note** you can anytime unlink and create new link pointing to newer version
