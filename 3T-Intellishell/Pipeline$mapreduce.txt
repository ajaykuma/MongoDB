db.getCollection("orders").aggregate(

	// Pipeline
	[
		// Stage 1
		{
			$group: {
			_id: "$cust_id", value: { $sum: "$price" } 
			}
		},

		// Stage 2
		{
			$out: // Note: must be last stage of pipeline
			"agg_alternative_1"
		},

	]

	// Created with Studio 3T, the IDE for MongoDB - https://studio3t.com/

);
